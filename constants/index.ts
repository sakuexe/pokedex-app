// import all the components from their own files
import { COLORS, SIZES, TYPE_COLORS } from "./theme";
import ICONS from "./icons";
import IMAGES from "./images";

// and export them as a single export object
export { COLORS, SIZES, ICONS, IMAGES, TYPE_COLORS };
